{
  "rules": [
    {
      "allowed": {
        "origins": [
          "http://localhost:3000",
          "https://tikea-saas-project.netlify.app"
        ],
        "methods": ["GET", "PUT"],
        "headers": ["*"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
